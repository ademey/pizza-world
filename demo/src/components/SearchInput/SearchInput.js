import React from "react";
import "./search-input.css";

const SearchInput = ({ onSearch }) => (
  <div className="pw-search-input">
    <input type="text" />
    <button onClick={() => {}}>></button>
  </div>
);

export default SearchInput;
